# ── Streamlit UI (deployed to Streamlit Cloud) ───────────────────────────────
streamlit==1.41.1
SQLAlchemy==2.0.36
python-dotenv==1.0.1
tenacity==9.0.0
rich==13.9.4
pandas==2.2.3

# ── Pipeline / scraping (run locally) ────────────────────────────────────────
# Install these locally: pip install -r requirements-pipeline.txt
