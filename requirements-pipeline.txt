# Full dependencies for running the pipeline locally
# pip install -r requirements-pipeline.txt
# playwright install chromium  (run once after install)

-r requirements.txt

# Web scraping
requests==2.32.3
playwright==1.49.0
beautifulsoup4==4.12.3
lxml==5.3.0

# Document extraction
pdfplumber==0.11.4
PyMuPDF==1.24.14
python-docx==1.1.2

# AI
anthropic==0.42.0
# OpenRouter is OpenAI-compatible; used only by llm_openrouter for the
# targeted-read extraction. Pipeline-only — the Streamlit service in
# requirements.txt never calls a model.
openai==1.59.6

# Pipeline status output (rich tables in pipeline.py)
rich==13.9.4

# Shared utilities (used by both insights and RFP)
tenacity==9.0.0

# Insights automation
pytest==8.3.3
freezegun==1.5.1
# Test-only: mocks R2's S3-compatible API for tests/test_pdf_store.py. Kept
# out of requirements.txt because render.yaml builds the public Streamlit
# service from that file, and moto[s3] would drag werkzeug, Jinja2,
# cryptography, responses, xmltodict and py-partiql-parser into it.
moto[s3]==5.2.2

# fastapi, uvicorn, pydantic, jsonschema and structlog were removed on
# 2026-08-16 with the RFP subsystem and its read API — no source imports remain.
httpx==0.27.2
