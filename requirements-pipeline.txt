# Full dependencies for running the pipeline locally
# pip install -r requirements-pipeline.txt
# playwright install chromium  (run once after install)

-r requirements.txt

# Web scraping
requests==2.32.3
playwright==1.49.0
beautifulsoup4==4.12.3
lxml==5.3.0

# Document extraction
pdfplumber==0.11.4
PyMuPDF==1.24.14
python-docx==1.1.2

# AI
anthropic==0.42.0

# Pipeline status output (rich tables in pipeline.py)
rich==13.9.4

# Shared utilities (used by both insights and RFP)
tenacity==9.0.0

# Insights automation
pytest==8.3.3
freezegun==1.5.1

# RFP pipeline + read API
# fastapi must track streamlit: both depend on starlette, and
# streamlit>=1.52 forces starlette>=0.40 which fastapi<0.116 rejects.
fastapi==0.139.0
uvicorn[standard]==0.32.0
pydantic>=2.6,<3
jsonschema==4.23.0
structlog==24.4.0
httpx==0.27.2
